import type { Metadata } from "next";
export const metadata: Metadata = { title: "Reçu de paiement", robots: { index: false, follow: false }, alternates: { canonical: null, languages: {} } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
