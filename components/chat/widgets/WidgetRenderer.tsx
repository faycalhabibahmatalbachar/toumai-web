"use client";

/**
 * Point d'entrée historique, conservé pour les imports existants.
 * Le rendu réel vit dans `registry.tsx`.
 */
export { WidgetRenderer, resolveWidget, WIDGETS } from "./registry";
